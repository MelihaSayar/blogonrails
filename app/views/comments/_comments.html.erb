<h3>Yorumlar</h3>
<% @post.comments.each do |comment| %>
  <div style="margin-top:15px">
    <div class="card">
      <div class="card-body">
        <p><strong><%= comment.username %></strong>: <%= comment.body %>
          <%= link_to "[X]", [comment.post, comment],
                            method: :delete,
                            data: {confirm: 'Silmek istediğine emin misin?'} %></p>
      </div>
    </div>
  </div>
<% end %>
