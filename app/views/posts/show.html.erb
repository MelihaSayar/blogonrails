<div style="margin-top:20px">

<h2><%= @post.title %></h2>
<p><%= @post.body %></p>
<hr>
<%= link_to "Düzenle", edit_post_path(@post), class: 'btn btn-outline-secondary' %>
<%= link_to "Sil", post_path(@post),
                  method: :delete,
                  data: {confirm: 'Silmek istediğine emin misin'},
                  class: 'btn btn-outline-danger' %>


<hr>

<%= render 'comments/comments' %>
<%= render 'comments/form' %>
